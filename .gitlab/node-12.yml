build-12:
  extends: [.docker-build]
  parallel:
    matrix:
      - ALPINE_VERSION: "3.12"
        NODE_VERSION: ["12.21.0", "12.22.0", "12.22.1"]
  rules:
    - exists:
        - .gitlab/node-12.yml
